version: 2.1

jobs:
  say-hello:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: "Say hello"
          command: "echo bonjour samir!"
      - run:
          name: "print date"
          command: "ifconfig"

workflows:
  say-hello-workflow:
    jobs:
      - say-hello
