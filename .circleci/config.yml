version: 2.1

jobs:
  extraction:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout

  test:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: "test du code"
          command: |
            ls -l
            wc -l fichier.txt

workflows:
  checkout-test-workflow:
    jobs:
      - extraction
      - test:
          requires:
            - extraction
